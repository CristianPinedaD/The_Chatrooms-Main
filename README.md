# Simple Node.js Chatroom

## Installation Guide

* Install [Node.js](https://nodejs.org/en/).
* Install the dependencies using `npm install`.
* Start the server with `npm start` (or open `run.bat`).
* The server will be hosted at http://localhost:8000.
